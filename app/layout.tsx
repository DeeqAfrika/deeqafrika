import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Deeq M Afrika | Rebuild Somali Football",
  description:
    "A campaign to rebuild and modernise Somali football through open leadership and equal opportunity.",
  applicationName: "Deeq M Afrika Campaign",
  keywords: ["Deeq Afrika", "Somali football", "SFF", "Vision 2034", "Xiriir Furan", "Fursad Siman"],
  icons: {
    icon: "/favicon.png",
    shortcut: "/favicon.png",
    apple: "/favicon.png",
  },
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>{children}</body>
    </html>
  );
}
